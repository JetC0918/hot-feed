# Server-only local development values. Never expose these through client code.
PROTOTYPE_AUTH_EMAIL=
PROTOTYPE_AUTH_PASSWORD=
SESSION_SECRET=
API_KEY=

# Use at least 32 random characters for SESSION_SECRET.
# Production deployments should use encrypted platform secrets instead of this file.
