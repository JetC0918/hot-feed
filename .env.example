# Server-only values used by the Cloudflare Pages Function.
# Create a Reddit application and never expose these with a VITE_ prefix.
REDDIT_CLIENT_ID=
REDDIT_CLIENT_SECRET=
REDDIT_USER_AGENT=web:hot-feed:0.2.0 (by /u/your_reddit_username)
